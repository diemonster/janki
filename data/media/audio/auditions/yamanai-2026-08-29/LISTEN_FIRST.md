# Blind sentence-audio audition

Run: `yamanai-2026-08-29`

Listen to `A.wav`, `B.wav`, `C.wav`, and `D.wav` before opening `answer-key.json`. Rank pronunciation first, then naturalness and pace. In particular, note whether the verb is spoken as やまない.

These are audition artifacts only. None replaces the audio used by a deck.
