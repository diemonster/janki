# Onyx paired audition

Compare `A.wav` with `../yamanai-2026-08-29/A.wav` and `C.wav` with `../yamanai-2026-08-29/C.wav`. The model, sentence, instructions, API path, and output format are paired; only `marin` became the deck's `onyx` voice.

B and D are absent because OpenAI's current Realtime voice contract does not list `onyx`; no unsupported paid calls were attempted.
