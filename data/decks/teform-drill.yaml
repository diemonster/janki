deck:
  kind: conjugation
  form: te_form
  name: "Brandon Japanese::Te-form Practice"
  deck_id: 2059400114
  model_id: 1607392351
  model_name: "Japanese Pattern"
  description: "Everything janki can conjugate, drilled into its te-form. Answers computed by janki, never transcribed."
  # Not "every verb": membership is whatever `drill_cards` can build a te-form
  # for. A record whose structural label is unresolved (currently はく), or
  # whose stored group does not fit its expression (for example a bare
  # onomatopoeic stem marked `suru` but not ending in する), produces no card.
  # The deck is defined by the conjugation it can build, not by part of speech.
  output: "teform-drill.apkg"
